{
  "name": "despacho2-back",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "start:api": "node src/app.js",
    "pretest": "npm install",
    "test": "node --test ./test/pedidos.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
